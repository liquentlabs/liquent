# Rendered per-node configs (contains private keys via identity.yaml).
# Regenerated by setup-instance.sh on every run.
config/
config-*/

# Bench artifacts (rendered toml, deployed contract addresses, run logs).
bench/
bench-*/

# Long-running / RTT exploratory bench artifacts (runtime only).
bench-longrun*/
bench-rtt-*/

# Operator overrides.
.env
